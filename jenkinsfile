pipeline {
    agent any

    stages {

        stage('Clone Code') {
            steps {
                git 'git branch: 'main', url: 'https://github.com irk’'
                
            }
        }

        stage('Build Docker Image') {
            steps {
                script {
                    docker.build("qrapp")
                }
            }
        }

        stage('Run Container') {
            steps {
                script {
                    sh 'docker rm -f qr-container || true'
                    sh 'docker run -d -p 3000:3000 --name qr-container qr-app'
                }
            }
        }

    }
}